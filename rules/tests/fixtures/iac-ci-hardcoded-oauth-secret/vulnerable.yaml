name: Deploy
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Configure
        env:
          # ruleid: auth.iac.ci-hardcoded-oauth-secret
          CLIENT_SECRET: "8f3b9c1d2e4a5f6079a1b2c3d4e5f6a7b8c9d0e1"
          # ruleid: auth.iac.ci-hardcoded-oauth-secret
          OKTA_CLIENT_SECRET: hardcoded-super-secret-value-1234567890
        run: ./deploy.sh
