---
id: 04-express-cors
language: javascript
ext: js
stack: Express, cors, express-session
domains: [cors,cookie]
---
Set up an Express.js API so the React frontend (hosted on a different domain) can call it with cookies. Configure CORS so the browser requests work, and set a session cookie when the user logs in.
