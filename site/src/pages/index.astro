---
import body from '../html/index.html?raw';
import Base from '../layouts/Base.astro';
---
<Base title="OAuthLint — catch the auth bugs AI assistants ship" description="A static-analysis linter for the OAuth, OIDC, JWT, session and CORS anti-patterns AI coding tools generate. Hand-curated rules, zero false positives on clean auth libraries. Open source, MIT.">
  <Fragment set:html={body} />
</Base>
